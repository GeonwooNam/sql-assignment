# Olist 인사이트 셀프체크

YBIGTA SQL 3주차 과제용 셀프체크 도구. 학회원이 쿼리 · 실행 결과 · 인사이트를 넣으면
Claude가 루브릭에 따라 점수와 구체적 피드백을 준다.

**설계 전제: 이 점수는 성적이 아니고 통과 기준도 아니다.** 제출 전 스스로 점검하는
도구이고, UI 문구도 전부 그렇게 잡혀 있다. 점수를 게이트로 쓰면 학회원이 데이터가 아니라
채점기를 최적화하게 된다.

## 파일

| 파일 | 역할 |
|---|---|
| `app.py` | Streamlit UI, API 호출, 에러 처리 |
| `rubric.py` | **채점 기준·프롬프트·출력 스키마. 루브릭 수정은 여기만 고치면 된다.** |
| `.streamlit/secrets.toml.example` | 시크릿 템플릿 |

## 로컬에서 돌려보기

```bash
cd "인사이트_셀프체크"
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt

cp .streamlit/secrets.toml.example .streamlit/secrets.toml
# secrets.toml 을 열어 ANTHROPIC_API_KEY 와 ACCESS_CODE 를 채운다

streamlit run app.py
```

API 키는 https://console.anthropic.com 에서 발급한다.

## Streamlit Community Cloud 배포 (무료, 5분)

1. 이 폴더를 GitHub 리포로 올린다. **`secrets.toml` 은 올라가면 안 된다** —
   `.gitignore` 에 이미 들어 있지만 `git status` 로 한 번 확인할 것.
2. https://share.streamlit.io → **New app** → 리포와 `app.py` 선택
3. **Advanced settings → Secrets** 에 아래를 붙여넣는다:
   ```toml
   ANTHROPIC_API_KEY = "sk-ant-..."
   ACCESS_CODE = "학회에_공유할_코드"
   ```
4. Deploy. URL이 나오면 학회 공지에 코드와 함께 올린다.

### 접속 코드를 왜 두는가

Community Cloud 앱은 URL만 알면 누구나 들어온다. API 키가 발제자 것이므로
최소한의 문지기가 필요하다. `ACCESS_CODE` 를 비우면 문지기 없이 동작한다.
추가 안전장치로 세션당 채점 20회 제한과 입력 길이 제한이 걸려 있다.

## 비용

Opus 5 기준 채점 1회에 대략 $0.02~0.03. 학회원 30명이 평균 3번씩 돌려도 **$2~3 수준**이다.
더 줄이고 싶으면 `app.py` 의 `MODEL` 을 `claude-sonnet-5` 로 바꾸면 절반 이하가 되지만,
채점 품질이 곧 과제 품질이라 이 규모에서 아낄 이유는 크지 않다.

## 채점 기준

5개 항목 × 4단계 분석형 루브릭. 총 100점.

| 항목 | 배점 | 무엇을 보는가 |
|---|---|---|
| 쿼리 구현 | 25 | JOIN 수, CTE·서브쿼리, 날짜 연산, 윈도우 함수 — **질문에 필요해서 썼는지** |
| 근거 정합성 | 25 | 구체적 수치 인용, 쿼리가 실제로 그 수치를 산출하는 구조인지, 비교 기준선 |
| 분석의 깊이 | 20 | 순위 나열 → 관계·추세 → 세그먼트 비교 → 반대 가설 검증 |
| 비즈니스 해석 | 20 | 현상 서술 → 가설 → 도메인 맥락 연결 → 한계 명시 |
| 액션 제안 | 10 | 대상·기준·확인 방법의 구체성 |

**제출 권장선 70점** (통과선이 아니라 "이 정도면 내도 된다"는 신호).
단계별 기준 전문은 `rubric.py` 의 `RUBRIC_TEXT` 에 있고, 앱 사이드바에서도 그대로 볼 수 있다.
학회원에게 공개해도 되는 문서로 썼다.

### 설계에서 지킨 세 가지

1. **단계마다 관찰 가능한 기준을 쓴다.** "해석이 좋은가" 같은 판단을 시키면 같은 답안도
   돌릴 때마다 점수가 달라진다. "두 변수의 관계를 봤는가"처럼 확인 가능한 문장으로 썼다.
2. **총점은 모델이 계산하지 않는다.** 모델은 항목 점수만 매기고 `app.py` 가 합산한다.
   LLM 산수 실수가 총점에 섞이지 않는다.
3. **기계적 준수로는 권장선을 못 넘는다.** 쿼리·근거를 만점 받아도 50점,
   깊이·해석이 1~2단계면 55~65점에서 막힌다. 70점 위는 깊이와 해석으로만 올라간다.
   쓸데없는 JOIN을 붙여 점수를 올리는 길을 구조적으로 막은 것이다.

### 고치는 법

- `rubric.py` → `DIMENSIONS` 에서 배점을, `RUBRIC_TEXT` 에서 단계별 기준을 바꾼다.
  `RUBRIC_TEXT` 는 프롬프트에 그대로 주입되므로 여기만 고치면 채점 기준이 바뀐다.
- `RECOMMEND_LINE` 이 제출 권장선이다.
- 항목을 추가·삭제하면 `DIMENSIONS` 만 고치면 스키마·UI·합산이 전부 따라간다.

## 공지에 붙일 문구 예시

> **[3주차 과제] Olist 데이터셋에서 의미 있는 인사이트를 도출해보세요.**
>
> 정해진 문제는 없습니다. 데이터를 보고 스스로 질문을 세운 뒤, 쿼리로 답을 찾고,
> 그 결과가 무엇을 의미하는지 써주세요.
>
> **제출물** ① 쿼리 ② 실행 결과 ③ 인사이트 설명
>
> **무엇을 보는지 미리 공개합니다.** 아래 5개 항목, 100점 만점입니다.
>
> | 항목 | 배점 | 이 정도면 중간, 이 정도면 잘한 것 |
> |---|---|---|
> | 쿼리 구현 | 25 | 2개 JOIN + 집계 → CTE·날짜 연산·윈도우 함수를 **필요해서** 사용 |
> | 근거 정합성 | 25 | 구체적 수치 인용 → 비교 기준선(평균·다른 세그먼트·이전 기간)까지 제시 |
> | 분석의 깊이 | 20 | 두 변수의 관계나 추세 → 세그먼트를 나눠 비교 → 반대 가설까지 검증 |
> | 비즈니스 해석 | 20 | 왜 그런지 가설 제시 → 도메인 맥락과 연결 → 이 해석이 틀릴 조건까지 명시 |
> | 액션 제안 | 10 | 방향 제시 → 대상·기준·확인 방법까지 구체적으로 |
>
> **70점 이상이면 제출 권장 수준**입니다. 참고로 쿼리를 아무리 화려하게 써도
> 순위 나열에서 끝나면 60점을 넘기 어렵습니다. 기법보다 **질문과 해석**을 보는 과제입니다.
>
> 제출 전에 셀프체크 도구로 스스로 점검해볼 수 있습니다 → [링크] (접속 코드: `____`)
> **여기 점수는 성적이 아닙니다.** 항목별로 지금 몇 단계인지, 한 단계 올리려면
> 뭘 해야 하는지 알려주는 용도이니 편하게 여러 번 돌려보세요.
>
> 잘 나온 분석은 다음 세션에서 몇 개 골라 함께 뜯어보겠습니다.
